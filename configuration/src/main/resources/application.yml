server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/5Rabie5/5r5-configuration.git

management:
  endpoints:
    web:
      exposure:
        include: "*"